"""Project-level constants.

Currently empty — add concrete constant namespaces here as features land
(e.g. third-party endpoint catalogs, magic strings shared across modules).
Log format constants live next to the configuration in
``src.core.utils.logging`` because the dependency rule forbids
``src.core`` from importing ``src.common``.
"""

from __future__ import annotations
